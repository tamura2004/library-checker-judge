name: Judge-Test

on:
  push:
    branches:
      - patch/ci

jobs:
  judge-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    
    - id: install crun
      name: Install crun
      run: ./packer/crun-install.sh
